rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Payment screenshots — public read/write for the no-login architecture
    match /payment-screenshots/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
