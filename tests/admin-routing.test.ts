// admin-routing.test.ts
import { render } from '@testing-library/react';
import { AdminRoute } from '../src/routes/AdminRoute';

describe('AdminRoute', () => {
  it('redirects non-admins', () => {
    // Stub
    expect(true).toBe(true);
  });
});
